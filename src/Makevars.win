# cran.r-project.org/doc/manuals/R-exts.pdf
PKG_CXXFLAGS=$(shell Rscript.exe -e "RcppArmadillo:::CxxFlags()")
PKG_LIBS = $(shell Rscript.exe -e "Rcpp:::LdFlags()") $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
